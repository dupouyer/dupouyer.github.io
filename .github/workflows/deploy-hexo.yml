name: Build and Deploy Hexo

# 触发条件：推送到 master 分支时自动构建
on:
  push:
    branches:
      - master
  # 允许手动触发工作流
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    # 1. 检出代码（包括子模块）
    - name: Checkout 🛎️
      uses: actions/checkout@v4
      with:
        submodules: recursive  # 递归克隆子模块（PublishNote）
        fetch-depth: 0         # 获取完整的 git 历史

    # 2. 设置 Node.js 环境
    - name: Setup Node.js 🔧
      uses: actions/setup-node@v4
      with:
        node-version: '18'     # 使用 Node.js 18 LTS
        cache: 'npm'           # 缓存 npm 依赖以加快构建速度

    # 3. 安装 Pandoc（用于某些 Hexo 插件的文档转换）
    - name: Install Pandoc 📄
      run: |
        sudo apt-get update
        sudo apt-get install -y pandoc

    # 4. 安装 Hexo CLI 和项目依赖
    - name: Install Dependencies 📦
      run: |
        npm install -g hexo-cli
        npm install

    # 5. 同步 PublishNote 文章到主目录
    - name: Sync PublishNote Articles 📝
      run: |
        # 从 PublishNote 子模块（根目录）复制所有 .md 文件到 _posts
        find PublishNote -name "*.md" -type f -exec cp {} source/_posts/ \;
        echo "✅ Synced $(find PublishNote -name "*.md" | wc -l) articles from PublishNote"

    # 6. 清理并生成静态站点
    - name: Build Hexo Site 🏗️
      run: |
        hexo clean
        hexo generate

    # 7. 部署到 gh-pages 分支
    - name: Deploy to gh-pages Branch 🚀
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./public
        publish_branch: gh-pages
        user_name: 'github-actions[bot]'
        user_email: 'github-actions[bot]@users.noreply.github.com'
        commit_message: 'Deploy Hexo site - ${{ github.event.head_commit.message }}'

    # 8. 构建摘要（可选）
    - name: Build Summary 📊
      if: success()
      run: |
        echo "### 🚀 Hexo Site Deployed Successfully!" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "**Commit:** ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY
        echo "**Branch:** ${{ github.ref_name }}" >> $GITHUB_STEP_SUMMARY
        echo "**Author:** ${{ github.actor }}" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "🌐 Site will be available at: https://dupouyer.github.io" >> $GITHUB_STEP_SUMMARY

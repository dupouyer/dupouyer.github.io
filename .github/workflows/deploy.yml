name: Build and Deploy Astro

# 触发条件：推送到 master 分支时自动构建
on:
  push:
    branches:
      - master
  # 允许手动触发工作流
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    # 1. 检出代码（包括子模块）
    - name: Checkout 🛎️
      uses: actions/checkout@v4
      with:
        submodules: recursive  # 递归克隆子模块（PublishNote）
        fetch-depth: 0         # 获取完整的 git 历史

    # 2. 安装 pnpm
    - name: Setup pnpm 🔧
      uses: pnpm/action-setup@v4
      with:
        version: 9

    # 3. 设置 Node.js 环境
    - name: Setup Node.js 🔧
      uses: actions/setup-node@v4
      with:
        node-version: '18'     # 使用 Node.js 18 LTS
        cache: 'pnpm'          # 缓存 pnpm 依赖以加快构建速度
        cache-dependency-path: 'Site/Astro/pnpm-lock.yaml'

    # 4. 安装依赖
    - name: Install Dependencies 📦
      run: |
        cd Site/Astro && pnpm install --frozen-lockfile

    # 5. 初始化和验证 PublishNote 子模块
    - name: Setup Submodules 📚
      run: |
        echo "Checking submodule status..."
        git submodule status
        echo "Initializing submodules..."
        git submodule update --init --recursive
        echo "Verifying PublishNote..."
        ls -la PublishNote/
        echo "Found $(find PublishNote -name "*.md" | wc -l) markdown files"

    # 6. 同步 PublishNote 文章到 Astro content 目录
    - name: Sync PublishNote Articles 📝
      run: |
        # 使用 convert-articles.js 处理文章：
        # - 解析并重建 frontmatter（保留原始标题、日期等元数据）
        # - 转换 categories/tags 为 YAML 数组格式
        # - 复制关联的图片目录到 content/blog
        node convert-articles.js

    # 7. 构建静态站点
    - name: Build Astro Site 🏗️
      env:
        STATIC_BUILD: 'true'
      run: |
        cd Site/Astro && pnpm run build

    # 8. 部署到 gh-pages 分支
    - name: Deploy to gh-pages Branch 🚀
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./Site/Astro/dist
        publish_branch: gh-pages
        user_name: 'github-actions[bot]'
        user_email: 'github-actions[bot]@users.noreply.github.com'
        commit_message: 'Deploy Astro site - ${{ github.event.head_commit.message }}'

    # 9. 构建摘要（可选）
    - name: Build Summary 📊
      if: success()
      run: |
        echo "### 🚀 Astro Site Deployed Successfully!" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "**Commit:** ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY
        echo "**Branch:** ${{ github.ref_name }}" >> $GITHUB_STEP_SUMMARY
        echo "**Author:** ${{ github.actor }}" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "🌐 Site will be available at: https://dupouyer.github.io" >> $GITHUB_STEP_SUMMARY
